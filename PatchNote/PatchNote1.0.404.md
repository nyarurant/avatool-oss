# PatchNote 1.0.404

改善

- ライブインポート実行時に複数のパッケージを並行してスキャンするようにしました。これにより、初めてインポートするパッケージが多い場合の待ち時間が大幅に短縮されます。
- 一括インポートで VPM オートインストーラー形式のパッケージが含まれる場合、Unity に渡さず Avatool が直接 VPM パッケージをインストールするようにしました。これにより、Unity が途中で止まる問題が解消されます。
