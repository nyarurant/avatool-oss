# Patch Note 1.0.328

- インポート前に依存ツール（Modular Avatar / liltoon / VRCFury）の未導入チェックを追加し、導入して続行するか、警告を無視して続行するか、キャンセルするかを選べるようにしました。
- 依存ツールチェック対象に `Poiyomi` と `Avatar Optimizer` を追加し、未導入時にインポート前警告で確認できるようにしました。
- 依存ツール推定の精度を改善し、`Assets/` だけでなく `Packages/` に含まれる構成もチェック対象にしました。
- インポート時のフォルダ名重複ケースで、リネーム未適用時でもアイコン反映が外れにくいようにフォールバック処理を改善しました。
